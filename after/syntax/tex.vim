" Add "empheq" to the list of math mode environments (as align, equation,
" gather, ...).
" See $VIMRUNTIME/syntax/tex.vim.
" call TexNewMathZone("Z", "empheq", 0)  " 0 -> don't define a starred version (empheq*)
